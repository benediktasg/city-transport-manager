require_relative 'fancy_record'

class Complaint < ActiveRecord::Base
  #not-migrated attribute :id, :user_id, :header, :content, :time, :status
  #not-migrated attribute :answer, :answerer_id
  

  
end
